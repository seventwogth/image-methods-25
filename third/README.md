# ЛР №3, часть 1

## Цель работы

Реализовать расчет RGB-яркости в заданной точке зеркально-диффузной поверхности для нескольких направлений наблюдения. В этой части работы не строится изображение: вычисляется только яркость в одной точке поверхности для набора положений наблюдателя.

## Краткая физическая постановка

Рассматривается одна точка поверхности `P_T`, для которой известны:
- положение точки в пространстве;
- нормаль к поверхности `N`;
- положение источника света и его RGB-интенсивность;
- параметры материала поверхности;
- несколько положений наблюдателя.

Для каждого положения наблюдателя нужно определить направление на источник света, направление на наблюдателя, отраженное направление падающего луча и затем вычислить итоговую яркость как сумму диффузной и зеркальной составляющих.

## Используемые формулы

### Отраженный луч

```text
O' = O - 2 * (O dot N) * N
```

Где:
- `O` - падающий луч;
- `N` - нормаль к поверхности;
- `O'` - отраженный луч.

### Локальная модель яркости

```text
L = L_diffuse + L_specular
L_diffuse = I * Kd_color * kd * max(0, N dot L_dir)
L_specular = I * Ks_color * ks * max(0, R dot V)^shininess
```

Где:
- `I` - RGB-интенсивность источника света;
- `Kd_color` - диффузный цвет материала;
- `Ks_color` - цвет зеркальной составляющей;
- `kd` - коэффициент диффузного отражения;
- `ks` - коэффициент зеркального отражения;
- `N` - нормаль поверхности;
- `L_dir` - направление от точки поверхности к источнику;
- `R` - отраженное направление падающего света;
- `V` - направление от точки поверхности к наблюдателю.

## Описание входных данных

Для хранения сцены используются dataclass-структуры:
- `LightSource` - `position: Vec3`, `intensity: Vec3`;
- `SurfacePoint` - `position: Vec3`, `normal: Vec3`;
- `Material` - `diffuse_color: Vec3`, `specular_color: Vec3`, `kd: float`, `ks: float`, `shininess: float`;
- `SceneInput` - объединяет `light`, `surface_point`, `material`, `observer_positions: list[Vec3]`.

Функция `create_default_scene()` создает осмысленный пример:
- точка поверхности находится в начале координат;
- нормаль направлена вверх вдоль оси `Z`;
- источник света расположен сбоку и сверху;
- заданы 5 положений наблюдателя;
- параметры материала выбраны простыми и удобными для объяснения на защите.

## Алгоритм работы

1. Создать входную сцену через `create_default_scene()`.
2. Для каждого положения наблюдателя вычислить направление `L_dir` от точки поверхности к источнику.
3. Вычислить направление `V` от точки поверхности к наблюдателю.
4. Построить отраженный вектор `R` по формуле зеркального отражения.
5. Вычислить диффузную составляющую `L_diffuse`.
6. Вычислить зеркальную составляющую `L_specular`.
7. Получить итоговую RGB-яркость `L = L_diffuse + L_specular`.
8. Найти максимальную компоненту среди всех рассчитанных яркостей.
9. Построить нормализованные значения в диапазоне `0..1` для удобного сравнения результатов.
10. Вывести результаты в консоль и сохранить их в текстовый файл.

## Нормализация яркости

Основной расчет выполняется в абсолютных или условных яркостях, то есть в значениях, которые напрямую получаются из формул локальной модели.

Для удобного сравнения результатов между разными направлениями наблюдения дополнительно выполняется нормализация:
- ищется максимальная компонента среди всех рассчитанных RGB-яркостей;
- каждое значение делится на этот максимум;
- если максимум равен нулю, нормализованные значения остаются нулевыми.

Нормализация нужна только для представления результатов в относительном виде. Gamma correction не используется, потому что в этой части ЛР изображение еще не формируется.

## Инструкция запуска

Из корня репозитория:

```bash
python third/main.py
```

При запуске программа:
- создает сцену по умолчанию;
- вычисляет яркость для пяти положений наблюдателя;
- печатает сырые и нормализованные значения в консоль;
- сохраняет тот же отчет в `third/results/brightness_results.txt`.

## Запуск тестов

Из корня репозитория:

```bash
python -m unittest discover third/tests
```

## Пример результата

```text
Input scene:
Surface point: (0.0000, 0.0000, 0.0000)
Normal: (0.0000, 0.0000, 1.0000)
Light position: (2.0000, 2.0000, 3.0000)
Light intensity RGB: (1.0000, 0.9000, 0.8000)
Material diffuse color: (0.7000, 0.7000, 0.7000)
Material specular color: (1.0000, 1.0000, 1.0000)
Material coefficients: kd=0.6, ks=0.8, shininess=16.0

Results:
Observer P01 = (-3.0000, 0.0000, 2.0000) -> raw L(RGB) = (0.3316, 0.2984, 0.2653) -> normalized L(RGB) = (1.0000, 0.9000, 0.8000)
Observer P02 = (-1.5000, 2.0000, 2.5000) -> raw L(RGB) = (0.3056, 0.2750, 0.2445) -> normalized L(RGB) = (0.9216, 0.8295, 0.7373)
Observer P03 = (0.0000, 3.0000, 3.0000) -> raw L(RGB) = (0.3056, 0.2750, 0.2445) -> normalized L(RGB) = (0.9216, 0.8295, 0.7373)
Observer P04 = (1.5000, 2.0000, 2.5000) -> raw L(RGB) = (0.3056, 0.2750, 0.2445) -> normalized L(RGB) = (0.9216, 0.8295, 0.7373)
Observer P05 = (3.0000, 0.0000, 2.0000) -> raw L(RGB) = (0.3056, 0.2750, 0.2445) -> normalized L(RGB) = (0.9216, 0.8295, 0.7373)
```

## Краткий вывод

В результате реализован учебный Python-проект для первой части ЛР №3, который вычисляет яркость в заданной точке поверхности для нескольких направлений наблюдения по локальной модели освещения. Проект не выполняет рендеринг изображения, не использует трассировку сцены, не работает с OBJ и PPM, а решает именно задачу ЛР №3-1: расчет яркости в заданных направлениях наблюдения.
